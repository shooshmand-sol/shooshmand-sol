on:
    pull_request:
        types: [opened, reopened, synchronize]

jobs:
    ci:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v3

            - name: Run unit tests
              run: |
                echo "Running unit tests..."
                sleep 10

            # - name: Send Dummy status check
            #   env:
            #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
            #   run: |
            #     gh api --method POST "/repos/${{ github.repository }}/statuses/${{ github.sha }}" -f state="success" -f context="Tests_Passed" -f description="No need to check for external tests in PRs"